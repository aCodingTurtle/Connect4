function [outBoard] = placeChip(scene, currentBoard, currentPlayer)
%PLACECHIP handles moving the marker and placing a chip into a valid column

% use a while loop and simple game engine

outBoard = currentBoard;
end